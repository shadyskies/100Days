#!/bin/bash

# username=''
# PAT=''
# while IFS="\n" read a b; do
    
# done < github_cred.txt
#     git status
#     git commit -m "$1" 
#     git push "https://$a:$b@100Days.git/file.git" --all

git commit -m "$1"
git push origin master